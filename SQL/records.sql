SELECT *
FROM emp
LIMIT 1 OFFSET 2;

SELECT *
FROM emp
LIMIT 3 OFFSET 4;

SELECT *
FROM emp
ORDER BY empno DESC
LIMIT 1;

SELECT DISTINCT sal
FROM emp
ORDER BY sal DESC
LIMIT 1 OFFSET 3;

SELECT *
FROM emp
WHERE sal = (
    SELECT DISTINCT sal
    FROM emp
    ORDER BY sal DESC
    LIMIT 1 OFFSET 2
);
